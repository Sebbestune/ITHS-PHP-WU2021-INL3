<?php

/*
 * G1: Skapa en klass "Vehicle" som innehåller attributen:
 * - name
 * - speed
 * - type
 * du ska även lägga till 2 egna valbara attribut fordon.
 * Lägg även till följande 3 metoder:
 * - printName()
 * - printSpeed()
 * - printType()
 * lägg även till 1 egen valbar metod som är relevant för fordon.
 */

/*
 * G2: Lägg till en konstruktor och destruktor i klassen
 * Konstruktorn ska ha defaultvärden för allt som inte sätts manuellt.
 * Destruktorn behöver inte göra något särskilt just nu.
 */

/*
 * G3: Lägg in lite skydd i klassens attribut och metoder.
 * Vi vill att speed och type och name ska vara protected så att vi behöver gå via funktionerna för att skriva ut dem.
 * Lägg även till en metod för att ändra speed: changeSpeedTo() som är public;
 */

/*
 * G4: Skapa en klass "Car" som ärver från "Vehicle". 
 * Overrida "printType()" metoden så att den skriver ut att det är en bil. E.g. "This is a car."
 * Overrida "printSpeed()" metoden så att den skriver ut hastighet i "km/h". E.g. "25 km/h".
 * Skapa ett objekt ifrån klassen bil som exemplifierar detta.
 */

/*
 * G5: Skapa en klass "Boat" som ärver från "Vehicle". 
 * Overrida "printType()" metoden så att den skriver ut att det är en båt. E.g. "This is a boat."
 * Overrida "printSpeed()" metoden så att den skriver ut hastighet i "knop". E.g. "20 knop".
 * Skapa ett objekt ifrån klassen båt som exemplifierar detta.
 */



?>